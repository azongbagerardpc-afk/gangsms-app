name: gangsms
description: GangSMS - Discussion privée entre amis
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  supabase_flutter: ^2.5.0
  shared_preferences: ^2.2.3
  image_picker: ^1.1.2
  record: ^5.1.2
  audioplayers: ^6.1.0
  intl: ^0.19.0
  path_provider: ^2.1.3
  cupertino_icons: ^1.0.6

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
